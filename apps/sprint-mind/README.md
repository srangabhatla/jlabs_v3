# 🚀 SprintMind

> PRD + JIRA hierarchy from one sentence

**Category:** Product  
**App ID:** `sprint-mind`  
**Route:** `/sprint-mind`

## What it does

SprintMind is part of the Janardhan Labs portfolio. It prd + jira hierarchy from one sentence.

## Files

| File | Purpose |
|------|---------|
| `App.jsx` | Main React component — all logic and styles |

## Development

From the repo root:

```bash
npm start
# navigate to /sprint-mind
```

## API

Uses the shared Vercel proxy at `/api/claude`.  
App ID for usage tracking: `sprint-mind`  
Auth: Supabase magic link via `AuthWrapper`
