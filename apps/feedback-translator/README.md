# 💬 FeedbackTranslator

> Decode what feedback actually means

**Category:** Communication  
**App ID:** `feedback-translator`  
**Route:** `/feedback-translator`

## What it does

FeedbackTranslator is part of the Janardhan Labs portfolio. It decode what feedback actually means.

## Files

| File | Purpose |
|------|---------|
| `App.jsx` | Main React component — all logic and styles |

## Development

From the repo root:

```bash
npm start
# navigate to /feedback-translator
```

## API

Uses the shared Vercel proxy at `/api/claude`.  
App ID for usage tracking: `feedback-translator`  
Auth: Supabase magic link via `AuthWrapper`
