# ✍️ StyleMirror

> Extract your voice. Rewrite anything in it.

**Category:** Writing  
**App ID:** `style-mirror`  
**Route:** `/style-mirror`

## What it does

StyleMirror is part of the Janardhan Labs portfolio. It extract your voice. rewrite anything in it..

## Files

| File | Purpose |
|------|---------|
| `App.jsx` | Main React component — all logic and styles |

## Development

From the repo root:

```bash
npm start
# navigate to /style-mirror
```

## API

Uses the shared Vercel proxy at `/api/claude`.  
App ID for usage tracking: `style-mirror`  
Auth: Supabase magic link via `AuthWrapper`
